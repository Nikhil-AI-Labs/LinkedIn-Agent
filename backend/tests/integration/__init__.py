"""Integration tests for LinkedIn AI Agent backend.

These tests require real external dependencies:
- PostgreSQL database (for checkpointer)
- LinkedIn credentials (for live API tests)

Run with: pytest tests/integration -v --integration
"""
